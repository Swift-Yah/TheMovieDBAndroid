apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

android {

    defaultConfig {
        consumerProguardFiles 'consumer-proguard-rules.pro'
    }

    def baseUrl = [
            debug  : '"https://api.themoviedb.org/"',
            release: '"https://api.themoviedb.org/"'
    ]

    def apiKey = [
            debug  : '"api_key"',
            release: '"api_key"'
    ]

    buildTypes {
        debug {
            buildConfigField 'String', 'BASE_URL', baseUrl.debug
            buildConfigField 'String', 'API_KEY', apiKey.debug
        }

        release {
            buildConfigField 'String', 'BASE_URL', baseUrl.release
            buildConfigField 'String', 'API_KEY', apiKey.release
        }
    }
}

final versions = rootProject.ext

dependencies {
    api "org.jetbrains.kotlin:kotlin-stdlib-jre7:$kotlin_version"

    // Support
    api "com.android.support:support-annotations:$versions.androidSupport"

    // Gson
    api "com.google.code.gson:gson:$versions.gson"

    // OkHttp
    implementation "com.squareup.okhttp3:okhttp:$versions.okHttp"
    implementation("com.squareup.okhttp3:logging-interceptor:$versions.okHttp") {
        exclude module: 'okhttp'
    }

    // Retrofit
    implementation("com.squareup.retrofit2:retrofit:$versions.retrofit") {
        exclude module: 'okhttp'
    }
    implementation("com.squareup.retrofit2:converter-gson:$versions.retrofit") {
        exclude module: 'retrofit'
        exclude module: 'gson'
    }

    // Log Tracking
    api "com.jakewharton.timber:timber:$versions.timber"

    // Android Arch
    api "android.arch.lifecycle:runtime:$versions.arch"
    api "android.arch.lifecycle:extensions:$versions.arch"

}